<!--
  The Web SDK, not the Worker SDK: the worker does the bulk of the ingesting, so
  it exposes its own health and metrics endpoints rather than being a black box.
-->
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <UserSecretsId>dotnet-BunkerFlow.Worker-ca827c1e-ad37-4af6-b64d-0f39b3e4b592</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\BunkerFlow.Integration\BunkerFlow.Integration.csproj" />
  </ItemGroup>
</Project>
